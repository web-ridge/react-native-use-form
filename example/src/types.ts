// Language enum for i18n support
export enum Language {
  EN = 'en',
  NL = 'nl',
}

// 🔥 No form types needed!
// The library automatically infers all types from initial values in useFormState()
